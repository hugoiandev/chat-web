import styled from "styled-components";

const NewRoomContainer = styled.div`
  width: 100vw;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const FormWrapper = styled.div`
  display: flex;
  justify-content: center;
  margin-top: 50px;
`;

const InputWrapper = styled.div`
  max-width: 300px;
`;

export { NewRoomContainer, FormWrapper, InputWrapper };
